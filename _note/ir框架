核心思路为，依靠builder类提供的API构建IR
API种类：
创建指令
创建类型
创建常量
创建变量

ir
类型系统（都继承自value)
常量：int float
寄存器变量：int float
指针（ssa后消除）:int float
数组
全局变量（最后再说）

print函数

创建函数
    插入module的链表和查找表

指令
irbuilder创建函数
setinsert
    插入到function里
    current指向现bb


irbuilder创建指令
allocate：
    创建allocate
    将allocate插入entry
    返回一个指针变量
    指针变量和allocate互相引用
    use/user就只在指令之间使用了

做成llvm形式，接在llvm后端上进行验证